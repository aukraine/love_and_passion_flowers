<!-- Portfolio Gallery Section -->
<section class="portfolio py-8">
  <h2 class="text-3xl text-gray-700 font-bold mb-6 text-center">Our Gallery</h2>
  <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
    <% @bouquets.each do |bouquet| %>
      <div class="portfolio-item">
        <%= link_to bouquet_path(bouquet) do %>
          <%= image_tag bouquet.photos.first, class: "w-full h-auto object-cover rounded-lg shadow-md mb-4", alt: bouquet.name %>
        <% end %>
      </div>
    <% end %>
  </div>
</section>

<!-- Buttons -->
<div  class="flex justify-center space-x-4">
  <!-- Back Button -->
  <%= link_to "Back to Landing page", root_path, class: "bg-gray-200 text-gray-700 text-lg font-semibold text-center px-4 py-2 rounded-md hover:bg-gray-300 shadow" %>
  <!-- New Button -->
  <%= link_to "Add a new Bouquet", new_bouquet_path, class: "bg-pink-100 text-pink-800 text-lg font-semibold text-center px-4 py-2 rounded-md hover:bg-pink-700 hover:text-white shadow" %>
</div>
